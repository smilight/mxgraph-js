# mxGraph Angular example

Steps:
1. Build the library in the repo root (`npm run build`).
2. In this folder run `npm install`.
3. Start dev server with `npm start` or build production with `npm run build`.
